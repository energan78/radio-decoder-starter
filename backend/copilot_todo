# 📡 Добавление автоматического определения типа сигнала по частоте

## 🎯 Цель
После загрузки IQ-файла определить, к какому типу радиосигнала относится частота (например, GSM, Wi‑Fi, GPS) и сгенерировать комментарий: "GSM 900 — сотовая связь (модуляция: GMSK)".

---

## ✅ Шаги

### 1. Создать файл: `radio_decoder/freq_db.py`

Создай список словарей с диапазонами частот:

```python
FREQ_DB = [
    {"range": (88, 108), "label": "FM-радио", "mod": "FM", "usage": "Гражданское вещание"},
    {"range": (118, 137), "label": "Авиасвязь", "mod": "AM", "usage": "Связь с воздушными судами"},
    {"range": (144, 148), "label": "Радиолюбители (2 м)", "mod": "FM, SSB", "usage": "Ham radio"},
    {"range": (433.05, 434.79), "label": "ISM 433 МГц", "mod": "FSK, LoRa", "usage": "Датчики, IoT"},
    {"range": (868, 870), "label": "ISM 868 МГц", "mod": "LoRa, FSK", "usage": "Европейская IoT-связь"},
    {"range": (890, 915), "label": "GSM 900", "mod": "GMSK", "usage": "Сотовая связь"},
    {"range": (2400, 2483.5), "label": "Wi‑Fi / Bluetooth", "mod": "OFDM, GFSK", "usage": "Беспроводная связь"},
    {"range": (1575.42, 1575.42), "label": "GPS L1", "mod": "DSSS", "usage": "Навигация (GPS)"},
    {"range": (3500, 3700), "label": "5G NR", "mod": "QAM, OFDM", "usage": "Мобильная связь 5G"},
]
```

---

### 2. Создать файл: `radio_decoder/match_freq_band.py`

Добавить функцию, которая по частоте возвращает информацию о диапазоне:

```python
from .freq_db import FREQ_DB

def match_frequency(freq_mhz):
    for band in FREQ_DB:
        low, high = band["range"]
        if low <= freq_mhz <= high:
            return {
                "label": band["label"],
                "modulation": band["mod"],
                "usage": band["usage"]
            }
    return {
        "label": "Неизвестно",
        "modulation": "?",
        "usage": "Не удалось определить диапазон"
    }
```

---

### 3. В `backend/file_loader.py` или `backend/main.py`

После загрузки файла и определения `freq_mhz`:

```python
from radio_decoder.match_freq_band import match_frequency

# Предположим, что частота уже определена, например:
freq_mhz = 936.0

# Получаем информацию о диапазоне
comment_data = match_frequency(freq_mhz)

# Строим комментарий
comment = f"{comment_data['label']} — {comment_data['usage']} (модуляция: {comment_data['modulation']})"

# Добавляем в объект
radio_file_info["comment"] = comment
radio_file_info["label"] = comment_data["label"]
radio_file_info["modulation"] = comment_data["modulation"]
radio_file_info["usage"] = comment_data["usage"]
```

---

### 4. В `viewer.html` (или другой HTML-странице просмотра)

Добавить поле `comment`:

```html
<tr>
  <td>{{ file.filename }}</td>
  <td>{{ file.freq_mhz }} MHz</td>
  <td>{{ file.label }}</td>
  <td>{{ file.modulation }}</td>
  <td>{{ file.comment }}</td>
</tr>
```

---

## 📌 Дополнительно

- Добавить автоматическое извлечение частоты из имени файла, если это ещё не реализовано (`gsm_936.wav` → `936.0 МГц`).
- В будущем можно добавить:
  - Автоматическое определение модуляции;
  - Визуализацию спектра с подсказкой диапазона;
  - Поддержку пользовательских диапазонов (через конфигурацию).

---

## 🔧 Подсказка Copilot'у

Добавь в начале `main.py` или `file_loader.py`:

```python
# TODO: Определить частоту файла, сопоставить с базой диапазонов
# и автоматически сгенерировать человекочитаемый комментарий о типе сигнала.
```
